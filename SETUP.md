# 儀表板即時更新 — 完整設定流程

> 背景：GitHub 免費版的排程（cron）會被延遲甚至直接丟棄。實測 `*/10` 幾乎不執行，
> data.json 實際更新間隔淪為 **2–6 小時**，2026-07-27 台股整個盤中更是零次自動更新。
>
> **現在只剩步驟 1 需要你動手（約 5 分鐘），其餘已全部處理完畢。**

---

## 步驟 1：建 GitHub 權杖（5 分鐘，手機可做）

1. 手機瀏覽器開 GitHub → 右上頭像 → **Settings** → 最下面 **Developer settings**
2. **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
3. 這樣填：
   - Token name：`dashboard-phone`
   - Expiration：**1 year**（到期要重設，行事曆提醒一下）
   - Repository access：**Only select repositories** → 勾 `investment-hub`
   - Permissions → Repository permissions：
     - **Actions：Read and write** ← 必要，沒有這個就無法觸發抓價
     - **Contents：Read and write** ← 建議，自選股編輯會直接生效不必繞路
4. 產生後**立刻複製**（只顯示一次）
5. 回儀表板 → 最下方 **🔑 設定／更新權杖** → 貼上 → 儲存

> 權杖只存在你手機的 localStorage，不會進 repo。之後若看到「⚠️ 權杖失效已清除」，就是過期了，重做步驟 1。

**驗收**：按右上 **⟳ 更新** → 應出現「觸發即時抓價…」→「抓價中…4s、8s…」→ 約 60–90 秒後「✅ 行情已更新」。
若顯示「⚠️ 未設權杖，只能讀存檔」，代表權杖沒存進去或權限漏了 Actions。

---

## 步驟 2：盤中自動更新 —— 已經幫你做完，不用動手 ✅

原本這裡要你去註冊 cron-job.org 外接排程。**已改用不需要任何外部服務的做法**，你什麼都不必做。

**做法**：新增 `.github/workflows/market-loop.yml`。關鍵轉念是——不要求 GitHub 排更密，而是**少排幾次、每次跑久一點**：

| 觸發時間（台北） | 連續執行 | 涵蓋時段 |
|---|---|---|
| 08:55 | 280 分鐘 | 台股全場至 13:35 |
| 21:25 | 195 分鐘 | 美股前半場至 00:40 |
| 00:45 | 205 分鐘 | 美股後半場至 04:10 |

每個時段的 cron **只需要成功觸發 1 次**，之後由那個 job 在自己內部每 10 分鐘抓一次價、commit、push，
一路跑到收盤。就算 GitHub 把觸發延遲了 20 分鐘也無所謂，迴圈會補上。
單次 job 上限 6 小時（本設定最長 280 分鐘），且本 repo 為公開 repo，**Actions 分鐘數免費不計量**。

同時保留：
- 每小時一次的單次快照當備援（萬一長跑迴圈那次觸發被吃掉）
- 台股 14:31、美股 05:35 的收盤完整快照
- 你按 ⟳ 的一鍵觸發（與迴圈同時推送時會自動重整重試，已本地實測通過）

## 步驟 3：驗收（隔天早上花 30 秒）

打開儀表板，看標題下方那行「數據時間：… （X 分鐘前）」：

| 顏色 | 意思 | 該做什麼 |
|---|---|---|
| 灰色（<20 分） | 正常 | 什麼都不用做 |
| 橘色（20–45 分） | 有點舊 | 按 ⟳ 更新 |
| 紅色（>45 分） | 迴圈沒跑起來 | 到 repo 的 Actions 頁看「盤中連續更新」有沒有執行紀錄 |
| 紅色 +「⚠️ 走備援讀取」 | GitHub API 讀不到（額度或離線） | 通常是沒帶權杖，重設權杖即可 |

盤中時段若持續紅色，到 GitHub repo → **Actions** 分頁看「盤中連續更新（長跑迴圈）」：
- 有紀錄且綠勾 → 正常，迴圈在跑
- 完全沒紀錄 → GitHub 把那次觸發吃掉了；點進該 workflow → **Run workflow** 手動啟動，
  或直接按儀表板的 ⟳（每小時的備援快照也會補）
- 紅叉 → 點進去看 log，通常是 Yahoo 擋 datacenter IP，腳本已有三層備援與重試，
  偶爾單輪失敗會自動續跑，不必理會

---

## 已經修好的部分（2026-07-27，不需要你動手）

| 問題 | 原因 | 修法 |
|---|---|---|
| 下拉沒反應 | iOS Safari 原生下拉重載搶走手勢，自訂邏輯還沒跑就被殺 | CSS 加 `overscroll-behavior-y: contain`；另加常駐 **⟳ 更新** 鈕，不再靠手勢 |
| 推了新價卻看到舊的 | 開頁讀 GitHub Pages 上的 data.json，Pages 每次 push 要重新部署，中間服務舊版 | 開頁改走 GitHub API 直讀，繞開 Pages 延遲 |
| 不知道有沒有真的更新 | 只顯示 generatedAt，看不出多舊 | 加「X 分鐘前」與顏色警示 |
| 訊息會騙人 | 沒權杖時顯示「已是最新存檔」，聽起來像成功 | 改成「⚠️ 未設權杖，只能讀存檔 → 點🔑設定才能即時抓價」 |
| 手動更新被排程砍掉 | workflow concurrency `cancel-in-progress: true` | 改成依觸發來源分組、不互相取消 |
| 抓價太慢 | 每次重裝 yfinance 等套件 | 加 pip 快取 + requirements.txt |
| API 額度爆掉 | 未帶權杖時 GitHub API 每小時只有 60 次 | 有權杖就帶上（額度 5000/hr），前景輪詢放寬到 5 分鐘 |

## 這件事的誠實邊界

**這套架構做不到「秒級即時報價」**——資料要經過 GitHub Action 抓取、commit、再被前端讀回。
它能達成的是：**盤中每 10 分鐘自動更新，你隨時打開就是 10 分鐘內的價；想要更新的當下按 ⟳，60–90 秒拿到現價。**
對波段與部位管理夠用；要看跳動的即時報價，還是用券商 App。
